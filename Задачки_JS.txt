Задача 1.
Найти среднее арифметическое чисел от 1 до 25 включительно, 
если оно больше 50-вывести на экран 'Неверное решение', 
если меньше - ничего не выводить

let x=0;  
    for (let i =1; i<=25; i++){ 
        x=x+i}
    z = x/25;
if (z>50){
console.log('Неверное решение');
}


Задача 2.
В этом задании вам нужно вычислить сумму всех четных чисел, 
встречающихся в ряду от 1 до числа (включительно), 
передаваемого в нашу функцию (переменная "а").

function testWhile(a) {
    var x=0;
    for (let i=1; i<=a; i++){
        if(i % 2 == 0) {
        x=x+i }
}
return x;
}


Задача 3.
В этом задании нужно 
вычислить факториал для числа, передаваемого в нашу функцию 
и вывести его с помощью команды return.

function testFactorial(a) {
    var x=1;
    for(let i= 1; i<=a; i++){
 x=x*i}
    return x;
}


Задача 4.
Найти количество "счастливых" билетов в автобусе, формат билета XXX-XXX. 
Счастливым считается билет, 
в котором сумма цифр до знака "-", равна сумме цифр после этого знака.

Let count = 0
for (let a=0; a<=9; a++){
   for (let b=0; b<=9; b++){
     for (let c=0; c<=9; c++){
      for (let x=0; x<=9; x++){
       for (let y=0; y<=9; y++){
        for (let z=0; z<=9; z++){
if (a+b+c == x+y+z){
count=count +1
}
}
}
}
}
}
}
console.log(count)